import React                 from 'react';
import styles                from './styles.scss';

const FooterComponent = () => (
    <footer className={ styles.footer }>
        &copy;... It is HK! What are you waiting to see here?!
    </footer>
);

export default FooterComponent;
